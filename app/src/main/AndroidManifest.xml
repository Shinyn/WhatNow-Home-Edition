<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matti.whatnow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/what_now_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/what_now_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".EditActivity"
            android:screenOrientation="sensorPortrait"/>
        <activity android:name=".RandomActivity"
            android:screenOrientation="sensorPortrait"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!-- La till screenOrientation sensorPortrait.
     Portrait funkar men inte sensorPortrait -->
<!-- Ändrade ikonen, nu finns det tre versioner, androids basic icon, svart text på grön bakgrund
     ikon och en egen bild ikon-->